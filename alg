Программа, заполняющие узлы сети в Zabbix:

Считать номера коммутаторов во всей сети
Написать скрипт, добавляющий эти узлы в Zabbix

решение:

Библиотека Zabbix API для Go

golang-stats-api-handler -> позволяет получать

github.com/adubkov/go-zabbix -> получать и отдалять
